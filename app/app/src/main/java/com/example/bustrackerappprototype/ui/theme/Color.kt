package com.example.bustrackerappprototype.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)


// App themed color

// Brand / Primary
val   Primary = Color(0xFFE23744)        // Cranberry Red
val   PrimaryDark = Color(0xFFCB202D)    // Slight variation / darker red if needed

// Neutral / Background / Surface
val   NeutralDark = Color(0xFF2D2D2D)     // Peppercorn / dark gray
val   NeutralLight = Color(0xFFF4F4F2)    // Very light neutral (backgrounds, light surfaces)
val   White = Color(0xFFFFFFFF)           // White

// Text Colors
val   TextOnPrimary = Color(0xFFFFFFFF)   // White text on red primary
val   TextPrimary = Color(0xFF2D2D2D)      // Main text dark color
val   TextSecondary = Color(0xFF777777)    // A grey for subtext / captions (you might pick a grey consistent with style guide)

// Semantic / Status (You may define even if not part of   Zomato’s public branding, for UI intent)
val   Error = Color(0xFFD32F2F)           // Example: a red-error color
val   Warning = Color(0xFFFFA000)         // Amber / warning
val   Success = Color(0xFF388E3C)         // Green / success
val   Info = Color(0xFF1976D2)            // Blue / informational

// Border / Divider
val   Divider = Color(0xFFDDDDDD)         // Light-grey divider


